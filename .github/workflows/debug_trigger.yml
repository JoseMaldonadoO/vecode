name: Debug ActionTrigger

on: push

jobs:
  test-trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Say Hello
        run: echo "GitHub Actions está funcionando."
